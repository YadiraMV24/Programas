base = float(input("Introduce la base del rectangulo (en unidades): "))
altura = float(input("Introduce la altura del rectangulo (en unidades): "))


area = base * altura

print(f"El area del resctangulo es: {area} unidades cuadradas. ")

